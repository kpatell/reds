# System Instructions
You MUST read and strictly adhere to the rules defined in `AG_VALIDATION_RULES.md` before executing any tasks or generating code. It contains critical architecture, state management, and token optimization guidelines.

CRITICAL TOKEN EFFICIENCY RULE:
Always provide targeted surgical diffs instead of full file rewrites. Never output code that has not changed. Assume the user wants minimal token usage at all times.
